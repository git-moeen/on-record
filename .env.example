# Optional. When set, ask/enrich talk to a real Infona API
# (local OSS at http://localhost:8000, or hosted).
# Leave unset for fixture/demo mode — npm run dev still works.
INFONA_URL=
INFONA_API_KEY=
INFONA_TENANT=default
INFONA_KG=on-record
